# Reviewed secure-packer-bootstrapper release pin.
# This file is intentionally committed so PRs can update and review the
# bootstrapper release URL set and checksum in version control.
# Empty values are intentional until the first reviewed release exists.
# The privileged build fails closed until this file is refreshed with real pins.
# Managed by .github/scripts/refresh_secure_packer_bootstrapper_pin.py.
SECURE_PACKER_BOOTSTRAPPER_RELEASE_REPO=NWarila/secure-packer-bootstrapper
SECURE_PACKER_BOOTSTRAPPER_RELEASE_TAG=
SECURE_PACKER_BOOTSTRAPPER_RELEASE_URL=
SECURE_PACKER_BOOTSTRAPPER_RELEASE_SHA256=
SECURE_PACKER_BOOTSTRAPPER_RELEASE_SHA256_URL=
SECURE_PACKER_BOOTSTRAPPER_RELEASE_METADATA_URL=
